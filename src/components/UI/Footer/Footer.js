import React from "react";

const footer = () => {
  const styles = {
    backgroundColor: "#4CC4E0",
    width: "100%",
    height: "40px",
    color: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: "12px"
  };
  return <div style={styles}>info@bali-retirement.com</div>;
};

export default footer;
